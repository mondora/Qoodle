#documentiq-front

## Requirements:
	* node.js > v0.12.0.
			Install using nvm install v0.12.1 && nvm use v0.12.1
	* npm i && bower i

## Starts with
	* ./start.sh dev or
	* node scripts/dev.server.js
